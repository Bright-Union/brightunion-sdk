export default class EaseApi {
    static fetchCoverables(): Promise<unknown>;
    static rangeArray(array: any): any;
    static fetchQuote(amount: number, currency: string, period: number, protocol: any): Promise<any>;
}
